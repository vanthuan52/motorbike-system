import { EnumStoreStatus } from '../enums/store.enum';

export const StoreDefaultStatus = [
  EnumStoreStatus.active,
  EnumStoreStatus.inactive,
];

export const StoreDefaultAvailableSearch = ['name', 'status'];
