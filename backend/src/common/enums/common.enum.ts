/**
 * Common status enum for entities with simple active/inactive states.
 * Use this instead of importing EnumUserStatus for non-user entities.
 */
export enum EnumStatus {
  active = 'active',
  inactive = 'inactive',
}
