import { PartTypeStatus } from "../types";

const STATUS_OPTIONS = [
  { label: 'Hoạt động', value: PartTypeStatus.ACTIVE },
  { label: 'Không hoạt động', value: PartTypeStatus.INACTIVE },
];

export default STATUS_OPTIONS;