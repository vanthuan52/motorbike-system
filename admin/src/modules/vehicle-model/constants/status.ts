import { ENUM_VEHICLE_MODEL_STATUS } from "../types";

const STATUS_OPTIONS = [
  { label: "Hoạt động", value: ENUM_VEHICLE_MODEL_STATUS.ACTIVE },
  { label: "Không hoạt động", value: ENUM_VEHICLE_MODEL_STATUS.INACTIVE },
];

export default STATUS_OPTIONS;
