export enum EnumCareRecordStatus {
  pending = 'pending',
  inProgress = 'inProgress',
  completed = 'completed',
  cancel = 'cancel',
}

export enum EnumPaymentStatus {
  paid = 'paid',
  unpaid = 'unpaid',
}
