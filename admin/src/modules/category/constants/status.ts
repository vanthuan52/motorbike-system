import { ENUM_PART_TYPE_STATUS } from "../types";

const STATUS_OPTIONS = [
  { label: "Hoạt động", value: ENUM_PART_TYPE_STATUS.ACTIVE },
  { label: "Không hoạt động", value: ENUM_PART_TYPE_STATUS.INACTIVE },
];

export default STATUS_OPTIONS;
