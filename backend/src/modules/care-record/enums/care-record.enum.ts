export enum ENUM_CARE_RECORD_STATUS {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  DONE = 'done',
  CANCEL = 'cancel',
}

export enum ENUM_PAYMENT_STATUS {
  PAID = 'paid',
  UNPAID = 'unpaid',
}
