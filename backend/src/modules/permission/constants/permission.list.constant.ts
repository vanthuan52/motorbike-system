import { EnumPermissionGroup } from '../enums/permission.enum';

export const PermissionDefaultAvailableSearch = ['name', 'code', 'group'];
export const PermissionDefaultGroup = Object.values(EnumPermissionGroup);
