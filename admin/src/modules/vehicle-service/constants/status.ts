import { ENUM_SERVICE_CATEGORY_STATUS } from "../types";

const STATUS_OPTIONS = [
  { label: "Hoạt động", value: ENUM_SERVICE_CATEGORY_STATUS.ACTIVE },
  { label: "Không hoạt động", value: ENUM_SERVICE_CATEGORY_STATUS.INACTIVE },
];

export default STATUS_OPTIONS;
