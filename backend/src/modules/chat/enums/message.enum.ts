export enum EnumMessageType {
  text = 'text',
  image = 'image',
  video = 'video',
  file = 'file',
}

export enum EnumMessageStatus {
  sent = 'sent',
  delivered = 'delivered',
  read = 'read',
}
