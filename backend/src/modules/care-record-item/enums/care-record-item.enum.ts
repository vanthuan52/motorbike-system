export enum EnumCareRecordItemSource {
  scheduled = 'scheduled',
  additional = 'additional',
}

export enum EnumCareRecordItemType {
  part = 'part',
  service = 'service',
  upgrade = 'upgrade',
}
